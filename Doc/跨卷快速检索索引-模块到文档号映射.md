# 跨卷快速检索索引：模块 -> 文档号映射

> 目标：提供跨卷快速定位入口，按模块查对应文档号。  
> 口径：仅记录当前文档集可核验映射。

---

## 1. 核心执行与状态面

- 启动入口与主链路 -> `02`
- Query 执行内核 -> `03`
- Query/状态/远程协议细节 -> `13`
- 状态管理与上下文协同 -> `05`
- 任务运行时与输出 -> `17`
- 工具执行编排与流式调度 -> `24`

## 2. 命令、工具与权限面

- 命令系统与工具系统总览 -> `04`
- 命令/工具文件级索引 -> `12`
- 工具族参数边界与任务流 -> `15`
- 权限系统与安全闸门 -> `16`
- 会话摘要/MCP审批/防休眠/Token估算 -> `28`

## 3. 服务与平台能力面

- 服务子域与连接治理 -> `14`
- OAuth 认证与 API 客户端装配 -> `18`
- 远程托管设置与用户设置同步 -> `19`
- 上下文压缩与自动压缩策略 -> `20`
- Analytics 与插件后台安装链路 -> `21`
- LSP 与 TeamMemory 同步治理 -> `22`
- AutoDream 与 SessionMemory 后台记忆链路 -> `23`
- PromptSuggestion 与 MagicDocs 协作 -> `25`
- RateLimit 限额判定与提示链路 -> `26`
- Voice/通知/诊断/VCR -> `27`

## 4. 远程协同、Coordinator 与 Bridge 面

- Coordinator 模式与远程消息权限桥接 -> `29`
- Remote 会话管理/WS 传输/事件投递 -> `30`
- Bridge 初始化/消息路由/Env-less 核心 -> `33`
- Bridge 会话ID兼容/JWT刷新/可信设备 -> `34`
- Bridge 传输适配/门控判定/轮询配置 -> `35`
- Bridge 入站消息/附件/权限交互/状态UI -> `36`

## 5. 扩展生态与插件能力面

- Skills 加载/Bundled 技能/Builtin 插件 -> `31`
- 插件 CLI 命令与 SessionMemory 工具层 -> `32`

## 6. 模型与配置策略面

- 模型选择、别名校验与能力门控 -> `37`
- 协议字段与排障手册（含模型相关协议字段） -> `11`

## 7. 导航、方法与复核

- 全景与目录结构 -> `01`
- 设计思路与工程权衡 -> `06`
- 学习实践链路 -> `08`
- 研发架构深潜 -> `09`
- 专题详解（命令/工具/服务/状态） -> `10`
- 多轮检查记录 -> `07`
- 总导航 -> `00`

---

## 8. 使用建议

1. 先按模块定位文档号，再跳转到对应分卷。
2. 需要字段级排障时优先联查 `11 + 对应文件级分卷`。
3. 需要评估改动影响面时优先联查 `10 + 对应文件级分卷 + 07`。
